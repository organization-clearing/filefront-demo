1.设计方向：
	（1）.满足自身调度。（初始目的）
	（2）.无论是下载还是上传文件，前置front和核心core都无需参与操作，都是通过中端middle操作两者文件的交换。


2.job中execute应该是较高的抽象，而不是简单实现原理。
	应该强调是从前端接受文件，而以何种方式实现（FTP等）则是实现才需要考虑的事。

	
3.每一条业务线（Business），包含了若干种文件类型（例如：对账、核减、差异回盘、核减差异回盘等）。
	因此下载上传的时候应该先遍历业务线（Business）在遍历业务线特定文件的下载。
	
